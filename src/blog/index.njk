---
layout: layouts/base.njk
title: Blog
description: Thoughts on AI, ML, research, and software development.
eleventyExcludeFromCollections: true
permalink: false
---

<div class="py-12 md:py-16">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    {# Header #}
    <header class="mb-12">
      <h1 class="text-3xl md:text-4xl font-bold text-slate-900 dark:text-white mb-4">Blog</h1>
      <p class="text-lg text-slate-600 dark:text-slate-400">
        Thoughts on AI, ML, research, software development, and more.
      </p>
    </header>

    {# Posts List #}
    {% if collections.posts.length > 0 %}
      <div class="space-y-8">
        {% for post in collections.posts %}
          <article class="group">
            <a href="{{ post.url }}" class="neo-card neo-hover block p-6 no-underline">
              <div class="flex items-center gap-2 text-sm text-stone-600 dark:text-stone-400 mb-3">
                <time datetime="{{ post.date | isoDate }}">{{ post.date | readableDate }}</time>
                {% if post.data.tags %}
                  <span>&middot;</span>
                  <div class="flex flex-wrap gap-2">
                    {% for tag in post.data.tags %}
                      {% if tag != "posts" %}
                        <span class="neo-tag-accent">{{ tag }}</span>
                      {% endif %}
                    {% endfor %}
                  </div>
                {% endif %}
              </div>
              <h2 class="text-xl font-bold text-black dark:text-white font-display mb-2">
                {{ post.data.title }}
              </h2>
              {% if post.data.description %}
                <p class="text-stone-700 dark:text-stone-300">
                  {{ post.data.description }}
                </p>
              {% endif %}
            </a>
          </article>
        {% endfor %}
      </div>
    {% else %}
      <div class="text-center py-12">
        <p class="text-slate-600 dark:text-slate-400">No posts yet. Check back soon!</p>
      </div>
    {% endif %}
  </div>
</div>
