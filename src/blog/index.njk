---
layout: layouts/base.njk
title: Blog
description: Thoughts on AI, ML, research, and software development.
eleventyExcludeFromCollections: true
---

<div class="py-12 md:py-16">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    {# Header #}
    <header class="mb-12">
      <h1 class="text-3xl md:text-4xl font-bold text-slate-900 dark:text-white mb-4">Blog</h1>
      <p class="text-lg text-slate-600 dark:text-slate-400">
        Thoughts on AI, ML, research, software development, and more.
      </p>
    </header>

    {# Posts List #}
    {% if collections.posts.length > 0 %}
      <div class="space-y-8">
        {% for post in collections.posts %}
          <article class="group">
            <a href="{{ post.url }}" class="block p-6 bg-white dark:bg-slate-800 rounded-xl border border-slate-200 dark:border-slate-700 hover:border-primary-500 dark:hover:border-primary-500 transition-all no-underline">
              <div class="flex items-center gap-2 text-sm text-slate-500 dark:text-slate-400 mb-3">
                <time datetime="{{ post.date | isoDate }}">{{ post.date | readableDate }}</time>
                {% if post.data.tags %}
                  <span>&middot;</span>
                  <div class="flex gap-2">
                    {% for tag in post.data.tags %}
                      {% if tag != "posts" %}
                        <span class="text-primary-600 dark:text-primary-400">#{{ tag }}</span>
                      {% endif %}
                    {% endfor %}
                  </div>
                {% endif %}
              </div>
              <h2 class="text-xl font-semibold text-slate-900 dark:text-white group-hover:text-primary-600 dark:group-hover:text-primary-400 transition-colors mb-2">
                {{ post.data.title }}
              </h2>
              {% if post.data.description %}
                <p class="text-slate-600 dark:text-slate-400">
                  {{ post.data.description }}
                </p>
              {% endif %}
            </a>
          </article>
        {% endfor %}
      </div>
    {% else %}
      <div class="text-center py-12">
        <p class="text-slate-600 dark:text-slate-400">No posts yet. Check back soon!</p>
      </div>
    {% endif %}
  </div>
</div>
